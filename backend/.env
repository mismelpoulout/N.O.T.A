# ==============================================
# 🔧 CONFIGURACIÓN PRINCIPAL N.O.T.A BACKEND
# ==============================================

# --- Rutas internas ---
DATA_DIR=./backend/data
SQLITE_DB=./backend/data/medical.db
CORPUS_JSONL=./backend/data/corpus.jsonl

# ==============================================
# 🤖 MODELO LLM (Ollama / LM Studio / OpenAI-Compatible)
# ==============================================

# Servidor LLM local (Ollama expone /v1 compatible OpenAI)
LLM_BASE_URL=http://127.0.0.1:11434/v1

# Modelo ACTIVO (exactamente el que tienes en `ollama list`)
LLM_MODEL=llama3.1:8b-instruct-q4_K_M

# Clave “de cortesía” (Ollama no la exige, pero mantenla por compatibilidad)
LLM_API_KEY=ollama

# ==============================================
# ⚙️ PARÁMETROS DE SISTEMA
# ==============================================

# Caché de respuestas (TTL en segundos)
CACHE_TTL_SECONDS=86400

# ==============================================
# 🌍 Preferencias de Ranking / Búsqueda
# ==============================================

# Prioriza dominios confiables (separados por coma)
PREFERRED_DOMAINS=minsal.cl,intramed.net,medlineplus.gov,mayoclinic.org

# ==============================================
# 🚀 ENTORNO (usado en app.py)
# ==============================================
ENV=development
ENABLE_DOCS=true
ALLOWED_ORIGINS=*
TRUSTED_HOSTS=localhost,127.0.0.1
FORCE_HTTPS=false